{
  "name": "cwe-learning-app",
  "version": "1.0.0",
  "description": "Interactive CWE (Common Weakness Enumeration) learning application",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:check": "vue-tsc --noEmit && vite build",
    "preview": "vite preview",
    "preview:build": "npm run build && vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "prismjs": "^1.29.0",
    "vue": "^3.4.0",
    "vue-router": "^4.2.0"
  },
  "devDependencies": {
    "@types/prismjs": "^1.26.0",
    "@vitejs/plugin-vue": "^5.0.0",
    "typescript": "^5.3.0",
    "vite": "^6.4.2",
    "vue-tsc": "^3.2.7"
  }
}
